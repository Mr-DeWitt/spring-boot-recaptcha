# newer lombok versions don't generate constructor properties automatically so we have to set it manually
lombok.anyConstructor.addConstructorProperties=true

# don't visit parent directories for this is the root one
config.stopBubbling = true